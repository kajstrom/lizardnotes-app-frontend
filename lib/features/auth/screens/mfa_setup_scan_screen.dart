import 'package:flutter/material.dart';

class MfaSetupScanScreen extends StatelessWidget {
  const MfaSetupScanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('MFA Setup: Scan QR')));
  }
}
