import 'package:flutter/material.dart';

class MfaSetupVerifyScreen extends StatelessWidget {
  const MfaSetupVerifyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('MFA Setup: Verify OTP')));
  }
}
